请根据附图，在当前工作区的 `benchmark/output` 空目录中实现一个尽量还原设计稿的静态 Web 页面。

要求：

- 只使用 HTML、CSS 和原生 JavaScript，不安装依赖。
- 页面入口必须是 `index.html`，直接用浏览器打开即可运行。
- 还原整体布局、颜色、间距、卡片、图表、文字层级和侧边栏。
- 页面需要适配 1440×900 的桌面视口。
- 完成后自行检查生成的文件；不要向我索要设计参数。
